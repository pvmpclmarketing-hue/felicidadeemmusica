import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const headers={"Access-Control-Allow-Origin":"https://felicidadeemmusica.vercel.app","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Vary":"Origin","Content-Type":"application/json"};
Deno.serve(async(req)=>{if(req.method==="OPTIONS")return new Response("ok",{headers});if(req.method!=="POST")return new Response("Método não permitido",{status:405,headers});try{const {orderId}=await req.json();if(!orderId)return new Response(JSON.stringify({error:"Pedido inválido."}),{status:400,headers});const db=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);const {data,error}=await db.from("orders").select("status,honoree,music_url,delivery_mode").eq("id",orderId).eq("delivery_mode","download").single();if(error||!data)return new Response(JSON.stringify({error:"Pedido não encontrado."}),{status:404,headers});return new Response(JSON.stringify({status:data.status,honoree:data.honoree,musicUrl:data.music_url}),{headers});}catch{return new Response(JSON.stringify({error:"Não foi possível consultar sua entrega."}),{status:500,headers});}});
