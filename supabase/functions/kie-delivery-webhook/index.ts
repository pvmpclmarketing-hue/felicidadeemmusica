import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
const pick=(value:unknown):string|undefined=>{if(!value||typeof value!=="object")return;const obj=value as Record<string,unknown>;for(const key of ["audioUrl","audio_url","streamAudioUrl","stream_audio_url"])if(typeof obj[key]==="string"&&obj[key])return obj[key] as string;for(const child of Object.values(obj)){const found=pick(child);if(found)return found;}};
Deno.serve(async(req)=>{if(req.method!=="POST")return new Response("Method not allowed",{status:405});const url=new URL(req.url);if(url.searchParams.get("secret")!==Deno.env.get("KIE_CALLBACK_SECRET"))return new Response("Unauthorized",{status:401});try{const payload=await req.json() as Record<string,unknown>;const data=(payload.data??payload) as Record<string,unknown>;const taskId=String(data.taskId??data.task_id??payload.taskId??"");const status=String(data.callbackType??data.status??payload.status??"").toLowerCase();const db=createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);if(status==="complete"||status==="success"){const musicUrl=pick(data);if(musicUrl)await db.from("orders").update({status:"ready",music_url:musicUrl}).eq("kie_task_id",taskId);}else if(status==="failed"||status==="error")await db.from("orders").update({status:"delivery_failed"}).eq("kie_task_id",taskId);return new Response("ok");}catch{return new Response("ok");}});
