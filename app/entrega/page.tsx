"use client";
import {useEffect,useState} from "react";
const base=process.env.NEXT_PUBLIC_SUPABASE_URL, key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY??process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;
export default function Entrega(){const [state,setState]=useState<{status?:string;honoree?:string;musicUrl?:string;error?:string}>({});const id=typeof window==="undefined"?"":new URLSearchParams(window.location.search).get("pedido")??"";useEffect(()=>{if(!id||!base||!key)return;const check=async()=>{const r=await fetch(`${base}/functions/v1/get-order-delivery`,{method:"POST",headers:{"content-type":"application/json",apikey:key,Authorization:`Bearer ${key}`},body:JSON.stringify({orderId:id})});setState(await r.json())};void check();const timer=setInterval(()=>void check(),6000);return()=>clearInterval(timer)},[id]);if(state.error||!id)return <main className="flow center"><h1>Não encontramos esta entrega.</h1></main>;if(state.status!=="ready"||!state.musicUrl)return <main className="flow center"><div className="v2-pulse">♫</div><p className="kicker">PAGAMENTO CONFIRMADO</p><h1>Estamos preparando sua música completa.</h1><p className="v2-copy">Assim que terminar, o download aparecerá automaticamente nesta página.</p><div className="v2-loader"><span/></div></main>;return <main className="flow center"><p className="kicker">SUA MÚSICA ESTÁ PRONTA</p><h1>Uma música para {state.honoree}</h1><div className="v2-player"><audio controls src={state.musicUrl}/></div><a className="primary download-link" href={state.musicUrl} download>Baixar minha música</a><p className="v2-copy">Guarde esta lembrança para sempre.</p></main>}
